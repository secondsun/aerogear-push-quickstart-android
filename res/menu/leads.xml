<?xml version="1.0" encoding="utf-8"?>

<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:id="@+id/menuLeadsAccepted">
        <item android:id="@+id/avalable_leads"
              android:title="@string/availables"
              android:icon="@drawable/ic_menu_avalable_leads"
              android:showAsAction="ifRoom"/>
    </group>
    <group android:id="@+id/menuAvalableLead">
        <item android:id="@+id/refresh"
              android:title="@string/refresh"
              android:icon="@drawable/ic_menu_refresh"
              android:showAsAction="ifRoom"/>
        <item android:id="@+id/leads_accepted"
              android:title="@string/accepted"
              android:icon="@drawable/ic_menu_leads_accepted"
              android:showAsAction="ifRoom"/>
    </group>
    <group android:id="@+id/menuLogout">
        <item android:id="@+id/logout"
              android:title="@string/logout"
              android:icon="@drawable/ic_menu_logout"
              android:showAsAction="ifRoom"/>
    </group>
</menu>